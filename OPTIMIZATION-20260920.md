# 官網與遊戲頁修正紀錄

依據「INTRVL Studio 網站優化建議清單」的 11 項需求：

1. 首頁拆分繁中、英文、韓文的靜態頁面，每頁只輸出該語言的內容；切換連結支援鍵盤及無 JavaScript 情況。
2. 各語言頁有獨立標題、描述、canonical、hreflang、Open Graph 與社群卡片；互動導覽、作品說明及無障礙標籤依頁面語言顯示。
3. 工作室介紹與合作區加入「200+ 話不斷更」及每週製作節奏。
4. 版權年份由 2025 起算，自動更新至目前年度。
5. 遊戲入口使用獨立純星星 SVG，常駐顯示、無陰影或底線；保留 10 月 10 日開放時間。
6. 沿用 WebP、壓縮音樂與遊戲 bundle，增加首場景 preload，補足舊存檔場景的預載，提供真實進度與失敗重試。
7. 手機直向可直接遊玩，保留橫向視角；操作按鈕至少 44×44px，彈窗可在短螢幕捲動。
8. 通關頁加入 IG、Threads、X 分享及插圖下載；分享原始通關插圖，並使用該插圖作為連結預覽。原生分享不支援時提供下載。
9. 沿用 v3 自動存檔，新增待確認章節成就保存，全破存檔繼續時返回結算。
10. 結局放置醒目的漫畫入口與箭頭動畫，減少動態偏好下停止動畫。
11. 每章完成立即顯示成就彈窗，可閱讀該話或繼續；移除每章分享，只保留通關插圖分享。

使用者已確認第 1～3 話閱讀網址尚未提供。目前閱讀入口明確顯示「漫畫即將公開」，待在 `assets/rpg/comic-links.js` 填入正式連結後啟用。

驗證：`tests/site-optimization.cjs`。包含三語、SEO 欄位、星星與倒數、對話／章節／結算存檔、分享與下載、320–844px 手機排版、載入重試及正常動態。

## 手機選單修正

直向開始對話後旋轉成橫向，原本的 modal 對話仍會讓背景工具列無法點按。對話改為不封鎖工具列的 dialog，地圖操作仍由遊戲狀態阻擋；圖鑑及其他彈窗中的按鍵也不會推進底層對話。更新遊戲資源版本，讓已造訪玩家取得修正。

回歸測試：`tests/rpg-hud-menu.cjs`，涵蓋直向轉橫向、直接橫向對話、自由移動、選單展開收合、圖鑑開關、反覆旋轉及對話期間的移動／鍵盤隔離。
